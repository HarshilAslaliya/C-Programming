#include <stdio.h>

main()
{
    circle = pie r ^ 2;
    square = length * length;
    rectaangle = width * lenth;
    triangle = height * base / 2;
    simple interest = p * t * r / 100;
    perameter circle = 2 * pie(3.14) * r
}